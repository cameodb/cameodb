### Plan (Phase 6) ###

# -------------------------------------------------------------------------
# CameoDB Configuration
# -------------------------------------------------------------------------

[node]
# Human-readable Node Configuration name for logs/dashboards (The technical ID is in node_identity.json)
label = "cameo-node-01"
# Topology awareness (e.g., "us-east-1a", "rack-2")
zone = "default"

[network.http]
bind_address = "0.0.0.0"
port = 9480
request_timeout_secs = 30
max_body_size_mb = 200
# Security: Strict allowed origins for production usage, or "*" for development
# cors_allowed_origins = ["https://admin.cameodb.com", "http://localhost:3000"]
cors_allowed_origins = ["*"]

[network.cluster]
# Internal Node-to-Node communication (Gossip/RPC via libp2p)
enabled = true
bind_address = "0.0.0.0"
port = 9580
cluster_name = "cameodb-cluster"

# Seed nodes for discovery.
# In internal networks, IP/Port is sufficient (PeerID verification is optional).
# "/ip4/10.0.1.5/tcp/9580/p2p/12D3KooWStC9q8jJ7f9sF7w6...(full_peer_id_hash)"
seed_nodes = [
    "/ip4/10.0.1.5/tcp/9580",
    "/dns4/seed-01.internal/tcp/9580"
]

[storage]
# The first path in this list is treated as the "Root" data directory.
# 'node_identity.json' will be stored here.
data_paths = ["./data/cameodb"]

disk_usage_threshold_percent = 90
wal_sync = true
wal_segment_size_mb = 64
default_batch_size = 1000

# -- Sharding Logic --
# 'num_shards_init' is ignored if data already exists on disk.
num_shards_init = 4
max_shards_per_node = 8

[search]
# Indexer (Write Path) Memory
indexer_memory_min_mb = 16
indexer_memory_max_mb = 256

# Global Memory Limits
total_memory_limit_mb = 2048
memory_pressure_threshold_percent = 80

# Concurrency
# 0 usually implies "auto-detect" based on available CPU cores
search_threads = 8