### Plan (Phase 5)
- [x] Add coordinator messages/state for peer events and counters (RoutingUpdated, PeerDiscovered, PeerLost, DialFailed).
- [ ] Emit events from swarm runtime into coordinator (DistributedCluster -> actor).
- [x] Expose peer/counter data via GetStatus response.

### Plan (Phase 6) ###

# -------------------------------------------------------------------------
# CameoDB Configuration
# -------------------------------------------------------------------------

[node]
# Path to the persistent identity file (contains Name, Keypair, VNodes).
# If this file does not exist, the system should generate it on startup.
identity_path = "./data/node_identity.json"
# Optional: Rack/Zone for future topology awareness (e.g., "us-east-1a")
zone = "default"

[network.http]
# Client-facing API
bind_address = "0.0.0.0"
port = 9480
request_timeout_secs = 30
max_body_size_mb = 200
# Security: Explicitly list allowed origins for production
cors_allowed_origins = ["*"]

[network.cluster]
# Internal Node-to-Node communication (Gossip/RPC via libp2p)
enabled = true
bind_address = "0.0.0.0"
port = 9580
cluster_name = "cameodb-cluster"
# List of seed nodes to join the mesh (e.g., ["/ip4/10.0.0.1/tcp/9580/p2p/..."])
seed_nodes = []

[storage]
# Directory for database files (SSTables/segments)
data_paths = ["./data/cameodb"]

# Storage safety settings
disk_usage_threshold_percent = 90
wal_sync = true
wal_segment_size_mb = 64
default_batch_size = 1000

# -- Sharding Logic --
# These values control capacity. 
# 'num_shards_init' is only used if the node is initializing for the very first time.
num_shards_init = 4
max_shards_per_node = 8

[search]
# Indexer (Write Path) Memory
indexer_memory_min_mb = 16
indexer_memory_max_mb = 256

# Global Memory Limits
total_memory_limit_mb = 2048
memory_pressure_threshold_percent = 80

# Concurrency
# 0 usually implies "auto-detect" based on available CPU cores
search_threads = 8